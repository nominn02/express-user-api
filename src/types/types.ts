export type User = {
name: string;
age: string;
userName: string;
email: string;
phoneNumber: string;
password: string;
userId?: number;
}

export type Todo = {
  id: number;
  desc: string;
  isComplete: boolean;
};